<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ============================================================= -->
    <!-- Estilo para o AppBarLayout padrão -->
    <!-- ============================================================= -->

    <!-- Estilo reutilizável para AppBars em diversas seções -->
    <style name="DefaultAppBarStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@color/bg_app</item>
        <item name="android:elevation">16dp</item>
        <item name="android:padding">12dp</item>
    </style>

    <!-- ============================================================= -->
    <!-- Estilo para Títulos de Seções Gerais -->
    <!-- ============================================================= -->

    <!-- Estilo reutilizável para títulos das seções (Sobre Mim, Formação, Projetos, Contato) -->
    <style name="TitleSectionStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- ============================================================= -->
    <!-- Estilos para a seção "Sobre Mim" (About) -->
    <!-- ============================================================= -->

    <!-- Estilo para parágrafos na tela "Sobre Mim" -->
    <style name="AboutParagraph">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">40dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:fontFamily">@font/inter_regular</item>
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
        <item name="android:lineSpacingExtra">12dp</item>
        <item name="android:lineSpacingMultiplier">1.5</item>
        <item name="android:textIsSelectable">true</item>
    </style>

    <!-- ============================================================= -->
    <!-- Estilos para a seção "Formação" (Education) -->
    <!-- ============================================================= -->

    <!-- Estilo para subtítulos da seção Educação -->
    <style name="SubtitleEducationStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:layout_marginBottom">24dp</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Estilo para parágrafos na tela "Formação". Similar ao About, com margem inferior ajustada. -->
    <style name="EducationParagraph">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">20dp</item>
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:fontFamily">@font/inter_regular</item>
        <item name="android:textAppearance">?android:attr/textAppearanceMedium</item>
        <item name="android:lineSpacingExtra">12dp</item>
        <item name="android:lineSpacingMultiplier">1.5</item>
        <item name="android:textIsSelectable">true</item>
    </style>

    <!-- ============================================================= -->
    <!-- EEstilos para a seção de Projetos -->
    <!-- ============================================================= -->

    <!-- Botões de paginação -->
    <style name="PaginationButton" parent="Widget.AppCompat.ImageButton">
        <item name="android:layout_width">48dp</item>
        <item name="android:layout_height">48dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:background">@drawable/btn_gradient_ripple</item>
        <item name="android:tint">@color/btn_pagination_selector</item>
        <item name="android:scaleType">centerInside</item>
    </style>

    <!-- ============================================================= -->
    <!-- Estilos para a seção de Contato -->
    <!-- ============================================================= -->

    <!-- Estilo de Dialog com fundo transparente e sem borda visível -->
    <style name="TransparentDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
    </style>

    <!-- Estilo para os subtítulos da seção de contato -->
    <style name="ContactTitleText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">sans-serif-black</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textSize">22sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Estilo para links de contato -->
    <style name="ContactLinkText">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:fontFamily">@font/inter_regular</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:textSize">16sp</item>
        <item name="android:drawablePadding">3dp</item>
        <item name="android:drawableTint">@color/dark_gray</item>
    </style>
</resources>
